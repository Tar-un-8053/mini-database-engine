
  # Mini-database engine frontend

  This is a code bundle for Mini-database engine frontend. The original project is available at https://www.figma.com/design/z3rB5zgcxtvxFYind7SVaG/Mini-database-engine-frontend.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  